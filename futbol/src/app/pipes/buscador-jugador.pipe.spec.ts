import { BuscadorJugadorPipe } from './buscador-jugador.pipe';

describe('BuscadorJugadorPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorJugadorPipe();
    expect(pipe).toBeTruthy();
  });
});
